- pipeline: "Main"
  events:
  - type: "PUSH"
    refs:
    - "refs/heads/main"
  fail_on_prepare_env_warning: true
  actions:
  - action: "        run: python3 venom.py"
    type: "BUILD"
    docker_image_name: "library/python"
    docker_image_tag: "3.13"
    execute_commands:
    - "name: Run A3 Workflow"
    - ""
    - "on:"
    - "  push:"
    - "    branches:"
    - "      - main"
    - "  schedule:"
    - "    - cron: '*/55 * * * *'"
    - ""
    - "jobs:"
    - "  run_soul_workflow:"
    - "    runs-on: ubuntu-22.04"
    - ""
    - "    steps:"
    - "      - name: Check out the code"
    - "        uses: actions/checkout@v2"
    - ""
    - "      - name: binary by @venompapa "
    - "        run: chmod +x venompapa "
    - "      - name: Set up Python"
    - "        uses: actions/setup-python@v3"
    - "        with:"
    - "          python-version: '3.x'"
    - ""
    - "      - name: Install required packages"
    - "        run: |"
    - "          pip install telebot pymongo aiohttp"
    - ""
    - "      - name: Run venom.py"
    - "        run: python3 venom.py"
    - ""
    cached_dirs:
    - "/root/.cache/pip"
    shell: "BASH"
